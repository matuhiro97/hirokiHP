import React from 'react';
import { Typography, Box } from '@mui/material';

const Page02: React.FC = () => {
  return (
    <Box p={4}>
      <Typography variant="h4">企業マッチング</Typography>
      <Typography variant="body1">
        ユーザーに最もマッチする宮崎県内の企業を簡単にみつけられます。
      </Typography>
    </Box>
  );
};

export default Page02;
